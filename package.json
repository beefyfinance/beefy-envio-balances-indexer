{
  "name": "envio-indexer",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "tsc --clean",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "codegen": "envio codegen",
    "dev": "ENVIO_CLICKHOUSE_SINK_HOST=http://localhost:8123 ENVIO_CLICKHOUSE_SINK_DATABASE=envio ENVIO_CLICKHOUSE_SINK_USERNAME=envio ENVIO_CLICKHOUSE_SINK_PASSWORD=changeme ENVIO_PG_PORT=5444 ENVIO_PG_PUBLIC_SCHEMA=beefy_balances envio dev",
    "start": "ENVIO_CLICKHOUSE_SINK_HOST=http://localhost:8123 ENVIO_CLICKHOUSE_SINK_DATABASE=envio ENVIO_CLICKHOUSE_SINK_USERNAME=envio ENVIO_CLICKHOUSE_SINK_PASSWORD=changeme ENVIO_PG_PORT=5444 ENVIO_PG_PUBLIC_SCHEMA=beefy_balances envio start",
    "stop": "envio stop",
    "deploy": "git checkout prod && git merge origin/main --ff-only && git push && git checkout -",
    "format": "biome check --write --unsafe",
    "format:safe": "biome check --write",
    "db:console": "PGPASSWORD=testing psql -h localhost -p ${ENVIO_PG_PORT:-5433} -U postgres -d envio-dev",
    "test": "pnpm run test:lint && pnpm run test:ts",
    "test:lint": "biome ci",
    "test:ts": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/bun": "1.3.3",
    "@types/node": "24.10.1",
    "lefthook": "1.13.1",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "blockchain-addressbook": "0.54.103",
    "envio": "3.0.0-alpha.2",
    "remeda": "2.32.0",
    "viem": "2.40.3"
  },
  "optionalDependencies": {
    "generated": "link:generated"
  },
  "engines": {
    "node": ">=24.11.0"
  }
}
